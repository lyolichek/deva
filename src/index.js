import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css';
import DevaApp from './DevaApp';

ReactDOM.render(
  <React.StrictMode>
    <DevaApp />
  </React.StrictMode>,
  document.getElementById('root')
);
